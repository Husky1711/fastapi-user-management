# Development Environment Configuration
# FastAPI User Management - Development Settings

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP__NAME=FastAPI User Management (Dev)
APP__VERSION=1.0.0-dev
APP__DESCRIPTION=Role-based multi-tenant user management system - Development
APP__DEBUG=true
APP__HOST=localhost
APP__PORT=9000
APP__RELOAD=true
APP__ENVIRONMENT=development

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB__URL=mysql+pymysql://root:Sandhya%40332@localhost:3306/fastapi_db
DB__ECHO=true
DB__POOL_SIZE=5
DB__MAX_OVERFLOW=10

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
REDIS__HOST=localhost
REDIS__PORT=6379
REDIS__DB=0
REDIS__PASSWORD=
REDIS__SOCKET_CONNECT_TIMEOUT=5
REDIS__SOCKET_TIMEOUT=5
REDIS__RETRY_ON_TIMEOUT=true

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Development secret key (NOT for production!)
JWT__SECRET_KEY=development-secret-key-not-for-production-use-only-32-chars-min
JWT__ALGORITHM=HS256
JWT__ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT__REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# RATE LIMITING CONFIGURATION (More lenient for development)
# =============================================================================
# Endpoint-specific rate limits (higher for development)
RATE_LIMIT__ENDPOINT_LIMITS__USERS_LIST__MINUTE=50
RATE_LIMIT__ENDPOINT_LIMITS__USERS_LIST__HOUR=500
RATE_LIMIT__ENDPOINT_LIMITS__USER_DETAIL__MINUTE=200
RATE_LIMIT__ENDPOINT_LIMITS__USER_DETAIL__HOUR=2000
RATE_LIMIT__ENDPOINT_LIMITS__SESSIONS__MINUTE=100
RATE_LIMIT__ENDPOINT_LIMITS__SESSIONS__HOUR=1000
RATE_LIMIT__ENDPOINT_LIMITS__SESSION_REVOKE__MINUTE=50
RATE_LIMIT__ENDPOINT_LIMITS__SESSION_REVOKE__HOUR=500
RATE_LIMIT__ENDPOINT_LIMITS__LOGIN__MINUTE=100
RATE_LIMIT__ENDPOINT_LIMITS__LOGIN__HOUR=1000
RATE_LIMIT__ENDPOINT_LIMITS__REFRESH__MINUTE=200
RATE_LIMIT__ENDPOINT_LIMITS__REFRESH__HOUR=2000
RATE_LIMIT__ENDPOINT_LIMITS__SIGNUP__MINUTE=50
RATE_LIMIT__ENDPOINT_LIMITS__SIGNUP__HOUR=500

# Global IP limits (more lenient for development)
RATE_LIMIT__GLOBAL_IP_LIMITS__MINUTE=10000
RATE_LIMIT__GLOBAL_IP_LIMITS__HOUR=100000
RATE_LIMIT__GLOBAL_IP_LIMITS__DAY=1000000

# Rate limiting behavior
RATE_LIMIT__FAIL_OPEN=true
RATE_LIMIT__ENABLE_USER_LIMITS=true
RATE_LIMIT__ENABLE_IP_LIMITS=true

# =============================================================================
# LOGGING CONFIGURATION (Verbose for development)
# =============================================================================
LOG__LEVEL=DEBUG
LOG__LOG_DIR=logs
LOG__MAX_FILE_SIZE=10485760
LOG__BACKUP_COUNT=3
LOG__ENABLE_CONSOLE=true
LOG__ENABLE_FILE=true
LOG__ENABLE_JSON=false
LOG__ROTATION_ENABLED=true
LOG__ROTATION_SCHEDULE=daily
LOG__RETENTION_DAYS=7

# =============================================================================
# SECURITY CONFIGURATION (Relaxed for development)
# =============================================================================
SECURITY__ENABLE_CORS=true
SECURITY__CORS_ORIGINS=["*"]
SECURITY__ENABLE_HTTPS_REDIRECT=false
SECURITY__SESSION_TIMEOUT_MINUTES=60
SECURITY__MAX_LOGIN_ATTEMPTS=10
SECURITY__LOCKOUT_DURATION_MINUTES=5
