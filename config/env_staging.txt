# Staging Environment Configuration
# FastAPI User Management - Staging Settings

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
APP__NAME=FastAPI User Management (Staging)
APP__VERSION=1.0.0-staging
APP__DESCRIPTION=Role-based multi-tenant user management system - Staging
APP__DEBUG=false
APP__HOST=0.0.0.0
APP__PORT=8000
APP__RELOAD=false
APP__ENVIRONMENT=staging

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Staging database URL (replace with actual staging values)
DB__URL=mysql+pymysql://staging_user:STAGING_PASSWORD@staging_host:3306/staging_db
DB__ECHO=false
DB__POOL_SIZE=10
DB__MAX_OVERFLOW=20

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
REDIS__HOST=staging-redis-host
REDIS__PORT=6379
REDIS__DB=0
REDIS__PASSWORD=STAGING_REDIS_PASSWORD
REDIS__SOCKET_CONNECT_TIMEOUT=5
REDIS__SOCKET_TIMEOUT=5
REDIS__RETRY_ON_TIMEOUT=true

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Staging secret key (different from production!)
JWT__SECRET_KEY=staging-secret-key-different-from-production-must-be-at-least-32-chars
JWT__ALGORITHM=HS256
JWT__ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT__REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# RATE LIMITING CONFIGURATION (Moderate for staging)
# =============================================================================
# Endpoint-specific rate limits (moderate for staging)
RATE_LIMIT__ENDPOINT_LIMITS__USERS_LIST__MINUTE=10
RATE_LIMIT__ENDPOINT_LIMITS__USERS_LIST__HOUR=100
RATE_LIMIT__ENDPOINT_LIMITS__USER_DETAIL__MINUTE=40
RATE_LIMIT__ENDPOINT_LIMITS__USER_DETAIL__HOUR=400
RATE_LIMIT__ENDPOINT_LIMITS__SESSIONS__MINUTE=20
RATE_LIMIT__ENDPOINT_LIMITS__SESSIONS__HOUR=200
RATE_LIMIT__ENDPOINT_LIMITS__SESSION_REVOKE__MINUTE=10
RATE_LIMIT__ENDPOINT_LIMITS__SESSION_REVOKE__HOUR=100
RATE_LIMIT__ENDPOINT_LIMITS__LOGIN__MINUTE=20
RATE_LIMIT__ENDPOINT_LIMITS__LOGIN__HOUR=200
RATE_LIMIT__ENDPOINT_LIMITS__REFRESH__MINUTE=40
RATE_LIMIT__ENDPOINT_LIMITS__REFRESH__HOUR=400
RATE_LIMIT__ENDPOINT_LIMITS__SIGNUP__MINUTE=10
RATE_LIMIT__ENDPOINT_LIMITS__SIGNUP__HOUR=100

# Global IP limits (moderate for staging)
RATE_LIMIT__GLOBAL_IP_LIMITS__MINUTE=5000
RATE_LIMIT__GLOBAL_IP_LIMITS__HOUR=50000
RATE_LIMIT__GLOBAL_IP_LIMITS__DAY=500000

# Rate limiting behavior
RATE_LIMIT__FAIL_OPEN=true
RATE_LIMIT__ENABLE_USER_LIMITS=true
RATE_LIMIT__ENABLE_IP_LIMITS=true

# =============================================================================
# LOGGING CONFIGURATION (Balanced for staging)
# =============================================================================
LOG__LEVEL=INFO
LOG__LOG_DIR=/var/log/fastapi-staging
LOG__MAX_FILE_SIZE=20971520
LOG__BACKUP_COUNT=5
LOG__ENABLE_CONSOLE=true
LOG__ENABLE_FILE=true
LOG__ENABLE_JSON=true
LOG__ROTATION_ENABLED=true
LOG__ROTATION_SCHEDULE=daily
LOG__RETENTION_DAYS=30

# =============================================================================
# SECURITY CONFIGURATION (Moderate for staging)
# =============================================================================
SECURITY__ENABLE_CORS=true
SECURITY__CORS_ORIGINS=["https://staging.yourdomain.com", "https://api-staging.yourdomain.com"]
SECURITY__ENABLE_HTTPS_REDIRECT=true
SECURITY__SESSION_TIMEOUT_MINUTES=45
SECURITY__MAX_LOGIN_ATTEMPTS=7
SECURITY__LOCKOUT_DURATION_MINUTES=10
